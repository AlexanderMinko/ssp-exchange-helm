apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "exchange.fullname" . }}-config
  labels:
    {{- include "exchange.labels" . | nindent 4 }}
data:
{{- range $k, $v := .Values.config }}
  {{ $k }}: {{ $v | quote }}
{{- end }}
